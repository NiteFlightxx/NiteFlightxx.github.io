---
import Layout from '../layouts/Layout.astro';
import App from '../App.tsx';
import { loadBlogArticles } from '../lib/blog';

const blogArticles = await loadBlogArticles();
---

<Layout>
  <App client:only="react" blogArticles={blogArticles} />
</Layout>
