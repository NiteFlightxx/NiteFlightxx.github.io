---
import Layout from '../layouts/Layout.astro';
import App from '../App.tsx';
import { loadKnowledge } from '../lib/content';

const knowledgeArticles = await loadKnowledge();
---

<Layout>
  <App client:load knowledgeArticles={knowledgeArticles} />
</Layout>
