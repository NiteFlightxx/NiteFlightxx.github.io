---
import Layout from '../layouts/Layout.astro';
import App from '../App.tsx';
import { loadKnowledge, loadLab } from '../lib/content';

const knowledgeArticles = await loadKnowledge();
const labExperiments = await loadLab();
---

<Layout>
  <App client:load knowledgeArticles={knowledgeArticles} labExperiments={labExperiments} />
</Layout>
